services:
  - type: web
    name: trpg-with-saveload
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: GOOGLE_GENERATIVE_AI_API_KEY
        sync: false
      - key: NODE_VERSION
        value: 20
